<?php foreach($result as $rec): ?>

    <h1><?=$rec['title'] ?></h1>
    <p><?=$rec['text']?></p>

<?php endforeach; ?>

<?php foreach($result as $rec): ?>

    <h1><?=$rec['title'] ?></h1>
    <p><?=$rec['text']?></p>

<?php endforeach; ?>

<?php foreach($result as $rec): ?>

    <h1><?=$rec['title'] ?></h1>
    <p><?=$rec['text']?></p>

<?php endforeach; ?>

<?php foreach($result as $rec): ?>

    <h1><?=$rec['title'] ?></h1>
    <p><?=$rec['text']?></p>

<?php endforeach; ?>

<?php foreach($result as $rec): ?>

    <h1><?=$rec['title'] ?></h1>
    <p><?=$rec['text']?></p>

<?php endforeach; ?>

