<?php
/**
 * Файл был сгенерирован с помощю Geany Qm Framework
 * Необходимо провести реорганизацию кода даного контролера.
 *
 * Boostrap.php предназначен для подключения каких либо файлов, скриптов, классов
 * не входящих общую структуру загрузок фреймворка. И обявление необходимых загрузок та настройка этих скриптов.
 *
 */

class Controller[[CONTROLLERNAME]] extends [[CONTROLLEREXTENDS]]{

    /** Временно хранит имена видов */
    public $view;

    /** Временно хранит имена видов */
    protected $tpl;

    /** Розпаковывать массивы в виде */
    protected $extracted = false;

    /** Конструктор задает последовательность загрузки методов */
    public function __construct()
    {
        parent::__construct();
    }


    /** Временно хранит имена видов */
    public function before()
    {
        #Code...
    }

    /** Временно хранит имена видов */
    public function after()
    {
        #Code...
    }

    /** Временно хранит имена видов */
    public function actionIndex()
    {
        #Code...
    }

    /** Временно хранит имена видов */
    public function myMethod()
    {
        #Code...
    }

}